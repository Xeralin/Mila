# Mila Qt

Qt Quick (PySide6) GUI for Mila. Shares the `mila/` core logic with the CLI.

## Run

```bash
qt/.venv/bin/python qt/main.py
```

## Setup from scratch

```bash
python3 -m venv qt/.venv
qt/.venv/bin/python -m pip install -r qt/requirements.txt
```
